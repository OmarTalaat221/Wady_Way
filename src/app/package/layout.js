import React from "react";

export const metadata = {
  title: "Travel Packages & Tours | WADI WAY",
  description:
    "Explore our wide range of travel packages and tours. Filter by destination, activity, duration, and price to find your perfect trip with WADI WAY.",
  icons: {
    icon: "/src/app/favicon.ico",
  },
};

const Layout = ({ children }) => {
  return <>{children}</>;
};

export default Layout;
